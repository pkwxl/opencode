[driver] 测试脚本已执行完毕(第 {{seq}} 次)。

- 脚本(已按序归档): {{script}}
- 退出码: {{code}};耗时 {{ms}}ms;超时: {{runTimeout}}
- 完整输出: {{out}} 与 {{err}}(直读文件判断,文件较大时分段读取,不要凭猜测下结论)

据此判断测试结果并继续: 需要修复就继续修复;需要再次测试时,把测试脚本重写(或修改后重写)到 tmp/test.sh 再结束本轮消息,driver 会再次执行并反馈;重跑同一测试可直接把 {{script}} 复制为 tmp/test.sh。
